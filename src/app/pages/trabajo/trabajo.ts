import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-trabajo',
  imports: [RouterLink],
  templateUrl: './trabajo.html',
  styleUrl: './trabajo.css'
})
export class Trabajo {

}